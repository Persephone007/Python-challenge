'Create a script that will loop through one year of stock data for each run and return the total volume each stock had over that year.


Sub Homework_Piranhahaha()

Dim Stock As String

Dim Stock_Total As Double
Stock_Total = 0

Dim Column As Integer
Column = i
lastRow = ws.Cells(Row.Count, 1).End(x1Up).Row

Dim Summary_Table_Row As Integer
Summary_Table_Row = 2

For i = 2 To lastRow

If Cells(i + 1).Value <> Cells(i, 1).Value Then
Stock = Cells(i, 1).Value
Stock_Total = Stock_Total + Cells(i, 7).Value

Range("I" & Summary_Table_Row).Value = Stock
Range("J" & Summary_Table_Row).Value = Stock_Total

Summary_Table_Row = Summary_Table_Row + 1

Stock_Total = 0

Else
Stock_Total = Stock_Total + Cells(i, 7).Value

End If
Next i





End Sub


'You will also need to display the ticker symbol to coincide with the total stock volume.